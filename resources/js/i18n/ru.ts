/**
 * Russian dictionary for the app-wide translator (see composables/useI18n.ts).
 * Keys are the original English UI strings. Keep entries grouped by area and
 * alphabetized within a group where practical. A missing key is not an error —
 * the English text is shown as-is — but every user-facing string should
 * eventually live here.
 */
export const ru: Record<string, string> = {};
